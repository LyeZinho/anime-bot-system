# Coolify-specific overrides
# Ports range: 6050-6059

version: '3.8'

services:
  postgres:
    ports:
      - "6050:5432"

  redis:
    ports:
      - "6051:6379"

  api:
    ports:
      - "6052:3071"

  bot:
    env_file:
      - .env

  dashboard:
    ports:
      - "6053:3173"

networks:
  default:
    name: coolify
